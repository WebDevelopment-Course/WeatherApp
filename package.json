{
  "name": "weatherproject",
  "version": "1.0.0",
  "description": "This is a web app which extracts weather data from Open Weather API and fetches to user",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sachish",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2"
  }
}
